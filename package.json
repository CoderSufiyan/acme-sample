{
  "name": "acme-sample",
  "version": "1.0.0",
  "private": true,
  "description": "Sample repo with Stripe usage for Apimend demo",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo no tests"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  }
}